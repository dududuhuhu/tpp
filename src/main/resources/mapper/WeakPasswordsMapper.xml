<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tpp.threat_perception_platform.dao.WeakPasswordsMapper">

    <resultMap id="BaseResultMap" type="com.tpp.threat_perception_platform.pojo.WeakPasswords">
            <id property="id" column="id" />
            <result property="password" column="password" />
    </resultMap>

    <sql id="Base_Column_List">
        id,password
    </sql>

    <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from weak_passwords
        where  id = #{id} 
    </select>
    <select id="selectAllWeakPasswords" resultType="java.lang.String">
        SELECT password FROM weak_passwords limit 5;
    </select>
    <select id="findAll" resultType="com.tpp.threat_perception_platform.pojo.WeakPasswords">
        SELECT
        <include refid="Base_Column_List" />
        FROM weak_passwords
    </select>
    <select id="findRulesBySearchTypeAndKeywords"
            resultType="com.tpp.threat_perception_platform.pojo.WeakPasswords">
        select
        <include refid="Base_Column_List" />
        from weak_passwords
        <where>
            <if test="searchType != null and searchType != '' and keywords != null and keywords != ''">
                and (
                <choose>
                    <when test="searchType == 'password'">
                        password =#{keywords}
                    </when>
                </choose>
                )
            </if>
        </where>
        order by id
    </select>
    <select id="selectByPsd" resultType="com.tpp.threat_perception_platform.pojo.WeakPasswords">
        select
        <include refid="Base_Column_List" />
        from weak_passwords
        where  password=#{password}
    </select>


    <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        delete from weak_passwords
        where  id = #{id} 
    </delete>
    <delete id="delete">
        delete from weak_passwords
        where  id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
    <insert id="insert" keyColumn="id" keyProperty="id" parameterType="com.tpp.threat_perception_platform.pojo.WeakPasswords" useGeneratedKeys="true">
        insert into weak_passwords
        ( id,password)
        values (#{id},#{password})
    </insert>
    <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="com.tpp.threat_perception_platform.pojo.WeakPasswords" useGeneratedKeys="true">
        insert into weak_passwords
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="id != null">id,</if>
                <if test="password != null">password,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="id != null">#{id},</if>
                <if test="password != null">#{password},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.tpp.threat_perception_platform.pojo.WeakPasswords">
        update weak_passwords
        <set>
                <if test="password != null">
                    password = #{password},
                </if>
        </set>
        where   id = #{id} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.tpp.threat_perception_platform.pojo.WeakPasswords">
        update weak_passwords
        set 
            password =  #{password}
        where   id = #{id} 
    </update>
</mapper>
